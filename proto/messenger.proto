syntax = "proto3";

package proto;

service Messenger {
  rpc Send (SendRequest) returns (SendResponse);
}

message SendRequest {
  string to = 1;
  string message = 2;
  string channel = 3;
}

message SendResponse {
  string status = 1;
  string error = 2;
}
